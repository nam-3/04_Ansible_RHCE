---
- name: 사용자 추가
  hosts: web2
  vars_files:
    - secret2.yml
  tasks:
    - name: secret을 사용한 user1 사용자 추가
      ansible.builtin.user:
        name: "{{ username }}"
        password: "{{ pwhash | password_hash('sha512') }}"
