---
- name: 사용자 추가
  hosts: web1
  vars_files:
    - secret.yml
  tasks:
    - name: secret을 사용한 user1 사용자 추가
      ansible.builtin.user:
        name: "{{ username }}"
        password: "{{ pwhash }}"